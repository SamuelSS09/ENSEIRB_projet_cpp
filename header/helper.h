#ifndef HELPER_H
#define HELPER_H

#include <vector>
#include <string>
#include <sstream>

namespace helper{

	// extern const int book_index;
	std::vector<std::string> split_string(const std::string& s, char delimiter);

}

#endif